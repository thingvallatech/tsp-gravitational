---
phase: 01-foundation-surface
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/city-generator.ts
  - src/lib/surface.ts
  - src/store/index.ts
  - src/store/city-slice.ts
  - src/store/surface-slice.ts
  - src/components/CityCanvas.tsx
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can generate cities from any of 6 presets and get valid positions in [0,1] space"
    - "Same seed + same preset always produces identical city layouts"
    - "Gravitational surface is computed with isolation-weighted wells (outlier cities produce deeper wells)"
    - "Zustand store holds city data and surface data, ready for consumption by renderers"
    - "Changing cities automatically triggers surface recomputation"
    - "User can see cities placed on screen as colored dots on a Canvas 2D element"
    - "User can select a preset and enter a seed to change the displayed city layout"
  artifacts:
    - path: "src/lib/city-generator.ts"
      provides: "City preset generators for all 6 patterns"
      exports: ["generateCities"]
    - path: "src/lib/surface.ts"
      provides: "Gravitational height field computation"
      exports: ["computeHeightField"]
    - path: "src/store/index.ts"
      provides: "Combined Zustand store"
      exports: ["useStore"]
    - path: "src/store/city-slice.ts"
      provides: "City state and actions"
      contains: "createCitySlice"
    - path: "src/store/surface-slice.ts"
      provides: "Surface state and recomputation"
      contains: "createSurfaceSlice"
    - path: "src/components/CityCanvas.tsx"
      provides: "Canvas 2D visualization of city positions"
      contains: "CityCanvas"
  key_links:
    - from: "src/lib/city-generator.ts"
      to: "src/lib/prng.ts"
      via: "imports mulberry32 for seeded generation"
      pattern: "import.*mulberry32.*from.*prng"
    - from: "src/lib/surface.ts"
      to: "src/lib/math-utils.ts"
      via: "imports computeIsolationWeights and computeCentroid"
      pattern: "import.*computeIsolationWeights.*from.*math-utils"
    - from: "src/store/city-slice.ts"
      to: "src/lib/city-generator.ts"
      via: "calls generateCities in setCities action"
      pattern: "generateCities"
    - from: "src/store/index.ts"
      to: "src/store/city-slice.ts"
      via: "composes city slice into combined store"
      pattern: "createCitySlice"
    - from: "src/store/index.ts"
      to: "src/store/surface-slice.ts"
      via: "composes surface slice into combined store"
      pattern: "createSurfaceSlice"
    - from: "src/components/CityCanvas.tsx"
      to: "src/store/index.ts"
      via: "subscribes to cities via useStore selector"
      pattern: "useStore.*cities"
    - from: "src/app/page.tsx"
      to: "src/components/CityCanvas.tsx"
      via: "renders CityCanvas with preset/seed controls"
      pattern: "import.*CityCanvas"
---

<objective>
Implement the 6 city preset generators, the gravitational surface height field computation, the Zustand store that wires them together, and a minimal Canvas 2D visualization so the user can see cities on screen.

Purpose: Complete the Phase 1 data layer and deliver a visible result -- city configurations that produce gravitational surfaces, displayed as a dot-plot with interactive preset/seed controls.
Output: Working city generation, surface computation, Zustand store, and a Canvas 2D city viewer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-surface/01-RESEARCH.md
@.planning/phases/01-foundation-surface/01-01-SUMMARY.md
@src/lib/types.ts
@src/lib/prng.ts
@src/lib/math-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement city preset generators</name>
  <files>src/lib/city-generator.ts</files>
  <action>
Create `src/lib/city-generator.ts` with a `generateCities(preset, seed, count)` function that dispatches to 6 preset generators. Follow the code from the research document (01-RESEARCH.md, Pattern: City Preset Generators section) closely.

The function signature:
```typescript
export function generateCities(preset: PresetName, seed: number, count?: number): City[]
```

Default count = 20 (use DEFAULT_CITY_COUNT from types.ts).

Implement all 6 generators:
- **random**: Cities placed uniformly in [0.1, 0.9] range (margin from edges)
- **clustered**: 3-5 cluster centers, cities scattered around them with 0.15 spread, clamped to [0, 1]
- **circular**: Cities evenly spaced on a circle (radius ~0.35) centered at (0.5, 0.5) with slight radial jitter
- **grid**: Cities on a square grid with slight position jitter (0.02)
- **star**: 5-armed star pattern, cities distributed along arms with jitter
- **spiral**: Cities along a 2-rotation spiral from center outward with jitter

Critical rules:
1. Create the PRNG at the TOP of generateCities using `mulberry32(seed)` -- pass the rng closure to each generator function
2. NEVER use `Math.random()` -- all randomness flows through the seeded rng
3. All city coordinates must be in [0, 1] range -- clamp if needed
4. Each city gets a sequential `id` starting from 0
5. Import types from `./types` and mulberry32 from `./prng`
  </action>
  <verify>
`npx tsc --noEmit` passes. Manually verify: calling `generateCities('random', 42)` twice returns identical arrays (seed determinism). All 6 presets produce cities with x,y in [0,1]. No usage of `Math.random()` in the file (search for it).
  </verify>
  <done>All 6 city presets generate valid city arrays in [0,1] space. Same seed + preset always produces identical output. No Math.random() usage anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Implement surface computation and Zustand store</name>
  <files>src/lib/surface.ts, src/store/index.ts, src/store/city-slice.ts, src/store/surface-slice.ts</files>
  <action>
Create 4 files:

**src/lib/surface.ts** -- Gravitational height field computation:
```typescript
export function computeHeightField(cities: City[], params: SurfaceParams): SurfaceData
```
Implementation:
1. Create a `Float32Array` of size `gridResolution * gridResolution`
2. Pre-compute isolation weights via `computeIsolationWeights(cities)` from math-utils
3. Compute centroid via `computeCentroid(cities)` from math-utils
4. Pre-compute `invTwoSigmaSq = 1 / (2 * kernelWidth * kernelWidth)` outside the loop (performance)
5. For each grid cell (gx, gy), map to world coords wx, wy in [0, 1]:
   - Lift term: `h = liftAlpha * euclideanDistance(wx, wy, cx, cy)`
   - For each city i: subtract `weights[i] * exp(-dist^2 * invTwoSigmaSq)` (Gaussian kernel)
6. Track minHeight and maxHeight during computation
7. Return `SurfaceData` object with heightField, gridResolution, minHeight, maxHeight
8. Handle edge case: if cities array is empty, return a flat zero surface

Import City, SurfaceParams, SurfaceData from `./types`. Import computeIsolationWeights, computeCentroid, euclideanDistance from `./math-utils`.

**src/store/city-slice.ts** -- City state slice:
Use Zustand v5 `StateCreator` pattern. State:
- `cities: City[]` (initially empty)
- `preset: PresetName` (initially 'random')
- `seed: number` (initially DEFAULT_SEED from types)
- `cityCount: number` (initially DEFAULT_CITY_COUNT from types)
- `setCities: (preset: PresetName, seed: number, count?: number) => void` -- generates cities and triggers surface recomputation by calling `get().recomputeSurface()` after setting cities

The `StoreState` type must be the union of CitySlice & SurfaceSlice. Export it.

**src/store/surface-slice.ts** -- Surface state slice:
State:
- `surfaceData: SurfaceData | null` (initially null)
- `surfaceParams: SurfaceParams` (initially DEFAULT_SURFACE_PARAMS)
- `recomputeSurface: () => void` -- reads cities and surfaceParams from store, calls computeHeightField, sets surfaceData
- `setSurfaceParams: (params: Partial<SurfaceParams>) => void` -- merges params, then calls recomputeSurface

**src/store/index.ts** -- Combined store:
Use Zustand v5 `create<StoreState>()(...)` with the curried form for TypeScript. Spread both slices. Export `useStore`.

Key wiring: `setCities` must call `recomputeSurface()` after updating cities so the surface stays in sync. Use `get()` to access cross-slice actions.

IMPORTANT: No file in `src/store/` should import from `three`, `react-three`, or any rendering library. Store files may import from `react` (for Zustand hooks) and from `src/lib/`.
  </action>
  <verify>
`npm run build` passes. `npx tsc --noEmit` reports zero errors. Verify the store wiring by checking that `setCities` calls `recomputeSurface` (read the source). Verify `surface.ts` uses Float32Array, tracks min/max height, and pre-computes invTwoSigmaSq.
  </verify>
  <done>Surface computation produces a Float32Array height field with isolation-weighted Gaussian wells and centerpoint lift. Zustand store combines city and surface slices. Calling setCities automatically recomputes the surface. All data is ready for Phase 2 renderers to consume via useStore selectors.</done>
</task>

<task type="auto">
  <name>Task 3: Create minimal Canvas 2D city visualization with controls</name>
  <files>src/components/CityCanvas.tsx, src/app/page.tsx</files>
  <action>
Create a minimal but functional visualization so Phase 1 delivers something visible. This is a simple Canvas 2D dot-plot, NOT the final 3D visualization (that comes in Phase 2).

**src/components/CityCanvas.tsx** -- Canvas 2D city viewer:
Create a `'use client'` React component that:
1. Renders an HTML `<canvas>` element, 600x600 pixels, with a dark background (#1a1a2e or similar)
2. Subscribes to the Zustand store via `useStore` to read `cities` array
3. Uses a `useEffect` to draw on the canvas whenever `cities` changes:
   - Clear canvas with dark background
   - Draw a light grid (subtle lines at 0.25 intervals) for spatial reference
   - For each city: draw a filled circle at `(city.x * canvasWidth, city.y * canvasHeight)`, radius 6px
   - Color cities with a gradient based on their index (use HSL: `hsl(index/total * 360, 80%, 60%)`) so different cities are visually distinct
   - Draw city id number next to each dot in small white text (10px font)
   - Draw a border around the canvas (1px solid gray)
4. Use `useRef<HTMLCanvasElement>` for canvas access, `useCallback` for the draw function
5. Handle empty cities array gracefully (draw "No cities generated" text centered on canvas)

**src/app/page.tsx** -- Update the placeholder page to include controls and the canvas:
Replace the placeholder content with a `'use client'` page that:
1. Imports and renders `<CityCanvas />` component
2. Adds a preset selector: `<select>` dropdown with all 6 PresetName options (random, clustered, circular, grid, star, spiral)
3. Adds a seed input: `<input type="number">` for entering the seed value
4. Adds a "Generate" button that calls `useStore.getState().setCities(preset, seed)` with the selected preset and seed
5. On initial mount (useEffect with [] deps), auto-generate with default preset='random' and seed=42 so the canvas isn't empty on first load
6. Style with Tailwind: center the canvas, put controls above it in a row with gap-4, use basic dark theme classes (bg-gray-900, text-white)
7. Show a title "TSP Gravitational Surface Solver" as h1 above the controls
8. Show city count below the canvas: "Cities: {n}" from the store

Keep it simple -- this is a Phase 1 proof-of-life, not the final UI. No fancy styling needed.
  </action>
  <verify>
`npm run build` passes. `npm run dev` starts and visiting http://localhost:3000 shows:
1. A title, preset dropdown, seed input, and Generate button
2. A 600x600 dark canvas with colored dots representing cities
3. Changing the preset or seed and clicking Generate updates the canvas with new city positions
4. The default view (random, seed 42) shows ~20 colored dots spread across the canvas
  </verify>
  <done>User can see cities placed on screen as colored dots. User can select from 6 presets via dropdown. User can enter a seed and click Generate to see deterministic city layouts. Phase 1 delivers a visible, interactive result.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0
2. `npx tsc --noEmit` reports zero errors
3. `src/lib/city-generator.ts` implements all 6 presets, uses mulberry32, never calls Math.random()
4. `src/lib/surface.ts` produces Float32Array height field with Gaussian wells weighted by isolation
5. `src/store/index.ts` exports useStore combining both slices
6. setCities triggers recomputeSurface (cross-slice wiring works)
7. No rendering library imports in src/lib/ or src/store/
8. Visiting localhost:3000 shows colored city dots on a Canvas 2D element
9. Preset dropdown and seed input update the displayed city layout
</verification>

<success_criteria>
- All 6 city presets generate deterministic, valid city arrays
- Surface computation produces isolation-weighted gravitational height field
- Zustand store holds both city and surface state with automatic recomputation
- All code compiles and builds without errors
- User can see cities on screen as colored dots on a canvas
- User can select presets and enter seeds to change the city layout interactively
- Data layer is complete and ready for Phase 2 visualization
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-surface/01-02-SUMMARY.md`
</output>
